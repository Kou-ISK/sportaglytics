/** Stored timelines include short live-recording segments, unlike the import wizard's source-file limit. */
export const MAX_PACKAGE_TIMELINE_CLIPS = 50_000;
export const MAX_PACKAGE_TIMELINE_SECONDS = 86_400;
